# Broadcasting Configuration for Deployment
BROADCAST_CONNECTION=reverb

# Reverb Configuration
REVERB_APP_KEY=your-reverb-key
REVERB_APP_SECRET=your-reverb-secret
REVERB_APP_ID=your-reverb-app-id
REVERB_HOST=localhost
REVERB_PORT=8080
REVERB_SCHEME=http

# Pusher Configuration (optional, only if you want to use Pusher)
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_ID=
PUSHER_APP_CLUSTER=
